import { AppenGenderIconDirective } from './appen-gender-icon.directive';

describe('AppenGenderIconDirective', () => {
  it('should create an instance', () => {
    const directive = new AppenGenderIconDirective();
    expect(directive).toBeTruthy();
  });
});
